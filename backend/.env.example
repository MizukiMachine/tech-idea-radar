# Default port the Express server listens on
PORT=3001

# ZAI GLM API Key (required for AI Engine)
ZAI_API_KEY=your-api-key-here

# Model to use (optional, defaults to glm-5)
LLM_MODEL=glm-5

# Base URL (optional, defaults to ZAI GLM API)
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
